{% extends 'NTEAntiquitasBundle::layout.html.twig' %}

{% block body -%}
    <a href="{{ path('modules') }}">Modules</a> > 
    <a href="{{ path('modules_show', {'id': entity.idFiche.idChapitre.id}) }}">{{ entity.idFiche.idChapitre.idModule|raw }}</a> > 
    <a href="{{ path('modules_show', {'id': entity.idFiche.idChapitre.idModule.id}) }}">{{ entity.idFiche.idChapitre|raw }}</a> > 
    <a href="{{ path('fiches_show', {'id': entity.idFiche.id}) }}">{{ entity.idFiche|raw }}</a>

    <h1>Notice lien : {{ entity.nom|raw }}</h1>

    <div class="row-fluid">
        <h3>Url</h3>
        {{ entity.url }}
    </div>

    <div class="row-fluid">
        <h3>Description</h3>
        {{ entity.description|raw }}
    </div>
{% endblock %}
